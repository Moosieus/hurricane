# Unclosed strings and interpolation
# Parser should mark error but continue

defmodule MidEditString do
  def unclosed_string do
    "hello world

  def unclosed_interpolation do
    "hello #{name

  def valid_function(x), do: x
end
